# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Build outputs
dist/
build/
*.log
*.tmp

# Environment files
.env
.env.local
.env.*.local

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Optional: ignore any local test artifacts
coverage/
.nyc_output/

# Mobile (Expo / React Native)
mobile/node_modules/
mobile/.expo/
mobile/.expo-shared/
mobile/web-build/
mobile/dist/
mobile/.eas/

# iOS native build outputs (if prebuilt)
mobile/ios/Pods/
mobile/ios/build/
mobile/ios/DerivedData/

# Android native build outputs (if prebuilt)
mobile/android/.gradle/
mobile/android/build/
mobile/android/app/build/
